<!--
<div class="container" data-controller="search medicament">
<div class="row justify-content-center">
  <div class="col-sm-8 my-3">
    <%# <%= form_with url: pharmacies_path, method: :get, class: "d-flex" do %>
      <%# <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Entre le medicament et la quantité"
        %>
      <%# <%= submit_tag "Search", name: "", class: "btn btn-primary" %> %>
    <%# <% end %> %> %> %>
  <%# </div>
</div> %>
<div class="d-flex", data-controller="toggle">
      <div class="search-navbar d-flex">
        <form action="/" method="get">
          <label for="localisation" name="localisation"></label>
          <input type="search" id="localisation" name="localisation" placeholder="Localisation" required data-action="click->toggle#search">
          <label for="nom de medicament" name="nom de medicament"></label>
          <input type="text" id="nom de medicament" name="nom de medicament" placeholder="Nom de medicament" required data-action="click->toggle#search">
          <label for="Quantité" name="Quantité"></label>
          <input type="text" id="Quantité" name="Quantité" placeholder="Quantité" required data-action="click->toggle#search">
          <label for="Quantité" name="Quantité"></label>
          <input type="submit", class="btn-submit-search-navbar d-none", data-toggle-target="togglableElement" %>
        </form>
      </div>
</div>
</div>
-->
